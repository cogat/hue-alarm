There are 2 runnable parts:

1) wakeup.py, a background process that checks a google calendar and schedules alarms

2) hue.py, a UI for controlling the light manually

To install, test both parts run independenly, then edit /etc/rc.local to add:

cd /home/pi/hue/
. ./env/bin/activate
screen -dmS wakeup ./wakeup.py
screen -S hue ./hue.py
